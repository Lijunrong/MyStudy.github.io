and or not 逻辑操作符

and 和 or都使用“短路求值”，也就是说，它们只会在需要时才去评估第二个参数

and：a and b 当a为真时，返回b的值，当a为假时,直接返回a的值，相当于a?b:a

or ：a or b  当a为真时，直接返回a,当a为假，才返回b的值，相当于a?a:b

not: 取非操作，值为真时，not操作则变为假，反之为真。
有一种常用的Lua赋值写法：
x = x or v ，它用于当x为空时，把v赋值给x，而当x不为空时，直接把原有的x赋值给x


