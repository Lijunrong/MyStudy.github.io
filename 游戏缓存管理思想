游戏中使用缓存的思想：
   
1 客户端在玩家登陆时向服务器发一条请求，服务器把游戏中常用的数据传过来 

2 客户端收到响应后，解析数据结构，使用table表保存起来，并使用服务端定义的数据结构名作为key值，响应的数据作为value值

3 客户端通过数据结构名索引相应的数据，就可使用该数据了

使用缓存的意义：

通过把常用数据缓存起来，可以避免频繁地向服务器请求数据，造成服务器的压力，也造成打开界面有延迟。这是空间换取时间的思想。


弊端：

加大了内存压力，如果缓存没有处理好，可能导致缓存压力过大
