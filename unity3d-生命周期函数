只有继承了MonoBehaivor的类才有生命周期函数，生命周期函数是系统定义好的，受系统调用，调用顺序不随在代码文件中位置不同而不同，调用顺序是固定的

所有的生命周期函数：

Awake():唤醒函数，游戏启动即调用，只调用一次

OnEnable():启用函数，在脚本被启用时调用，在游戏中可能会设置脚本可不可启用，因此可能会调用多次，默认启用时调用一次，

Start();开始函数，执行一次，在脚本已经激活后调用，也就是在onEnable()函数后调用，可以在此做一些初始化事情

FixedUpdate():固定更新函数，每间隔0.02秒调用一次，所有物理组件在此函数中更新状态

update();更新函数，每一帧调用一次，在此函数执行需要每帧更新的业务逻辑

LateUpdate():稍后更新函数，执行N次，在update函数后执行

onGUI():GUI渲染函数，在update函数前执行，执行次数是update函数的两倍

onDisable():事件禁用函数，在组件被禁用时调用，在onDestroy函数调用前被调用一次

onDestroy();销毁函数,当脚本所挂在的物体被销毁时调用，调用一次


函数调用顺序：

游戏启动---》Awake()唤醒函数----》onEnabled()函数----》start函数----》FixedUpdadte函数-----》update 更新函数---->LateUpdate 后刷新函数 ----》onDisbale禁用函数----》onDestroy销毁函数


